actions:
  - name: triggerWorkflowRun
    definition:
      kind: synchronous
      handler: {{NHOST_BACKEND_URL}}/v1/functions/trigger-workflow-run
      forward_client_headers: true
    permissions:
      - role: owner
      - role: editor
  - name: approveStep
    definition:
      kind: synchronous
      handler: {{NHOST_BACKEND_URL}}/v1/functions/approve-step
      forward_client_headers: true
    permissions:
      - role: owner
      - role: editor
